# React State and Arrays: Demo Start

This is the start version of the React State and Arrays Demo.

## Development

To work locally, please install the dependencies using `npm i` first.

Run `npm run dev` to start a development server and open [localhost:3000](http://localhost:3000) in a browser.
